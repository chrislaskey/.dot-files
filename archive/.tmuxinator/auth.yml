name: auth
root: ~/BlueBox/auth
socket_name: auth

windows:
  - editor:
      layout: main-horizontal
      panes:
        - vim
        - # bash
  - console: bundle exec rails console
  - server: bundle exec rails server -b 0.0.0.0 -p 3100
