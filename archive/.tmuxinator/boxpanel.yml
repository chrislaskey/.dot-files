name: boxpanel
root: ~/BlueBox/boxpanel
socket_name: boxpanel

windows:
  - editor:
      layout: main-horizontal
      panes:
        - vim
        - # bash
  - console:
      layout: main-horizontal
      panes:
        - bundle exec rails console
        - gulp
  - server: bundle exec rails server -p 3000
