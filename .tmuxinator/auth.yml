name: auth
root: ~/BlueBox/auth
socket_name: auth

windows:
  - editor:
      layout: main-horizontal
      panes:
        - vim
        - # bash
  - console:
      layout: main-horizontal
      panes:
        - bundle exec rails console
        - gulp
  - server: bundle exec rails server -p 3100
